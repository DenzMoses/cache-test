{
  "name": "turbo-project",
  "version": "1.0.0",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "dependencies": {
    "turbo": "^2.2.3",
    "typescript": "^5.2.0"
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "jest": "^29.0.0"
  }
}
